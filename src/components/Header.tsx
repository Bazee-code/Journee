import styled from "styled-components";

const H2 = styled.h2`
    text-align : center
`

const Header = () => {
    
    return (
        <div>
            <H2>Todo List App</H2>
        </div>
    )
}

export default Header;